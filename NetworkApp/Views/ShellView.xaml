<Window x:Class="NetworkApp.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NetworkApp.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="ShellView" Height="720" Width="1280">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <Storyboard x:Key="CloseMenu">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)" Storyboard.TargetName="GridMenu">
                <EasingDoubleKeyFrame KeyTime="0" Value="250"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.5" Value="0"/>
            </DoubleAnimationUsingKeyFrames>
           
        </Storyboard>
        <Storyboard x:Key="OpenMenu">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)" Storyboard.TargetName="GridMenu">
                <EasingDoubleKeyFrame KeyTime="0" Value="0"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.5" Value="250"/>
            </DoubleAnimationUsingKeyFrames>
            
        </Storyboard>
    </Window.Resources>
    <Window.Triggers>
        <EventTrigger RoutedEvent="ToggleButton.Unchecked" SourceName="menu">
            <BeginStoryboard x:Name="CloseMenu_BeginStoryboard" Storyboard="{StaticResource CloseMenu}"/>
        </EventTrigger>
        <EventTrigger RoutedEvent="ToggleButton.Checked" SourceName="menu">
            <BeginStoryboard Storyboard="{StaticResource OpenMenu}"/>
        </EventTrigger>
    </Window.Triggers>
    
    <!--start of the design-->

   
    <Grid>
        <!--page structure and definitions-->
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <!--end of page structure and definitions-->

        <!--banner on the top with menu button and exit minimize and maximaze -->
        <materialDesign:ColorZone  materialDesign:ShadowAssist.ShadowDepth="Depth2" Mode="PrimaryMid" Grid.Row="0" Grid.ColumnSpan="2" >
            <Grid>
                <!--<materialDesign:PopupBox PlacementMode="BottomAndAlignRightEdges" HorizontalAlignment="Right" Margin="10"/>-->
                <ToggleButton x:Name="menu" Width="35" Height="35" Style="{StaticResource MaterialDesignActionToggleButton}" HorizontalAlignment="Left">

                    <ToggleButton.Content>
                        <materialDesign:PackIcon Kind="Menu" Width="30" Height="30" />
                    </ToggleButton.Content>
                    <materialDesign:ToggleButtonAssist.OnContent>
                        <materialDesign:PackIcon Kind="MenuOpen" Width="30" Height="30"/>
                    </materialDesign:ToggleButtonAssist.OnContent>
                 </ToggleButton>
            </Grid>
        </materialDesign:ColorZone>
        <!--end of banner on the top with menu button and exit minimize and maximaze-->

        <!--the menu-->
        <Grid x:Name="GridMenu" Width="250" Grid.Row="1" Grid.Column="0" HorizontalAlignment="Left" Margin="-250 0 0 0" Background="White" RenderTransformOrigin="0.5,0.5">
            <Grid.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform/>
                    <TranslateTransform/>
                </TransformGroup>
            </Grid.RenderTransform>
            <StackPanel>
                <ListBox Padding="37 14" FontSize="15" >
                    <ListBoxItem>
                        <Expander  HorizontalAlignment="Stretch"  Header="dashboard">
                            <ListView HorizontalAlignment="Right"  ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                                <TextBlock   Margin="24,8,24,16" Text="Incident"/>
                                <TextBlock   Margin="24,8,24,16" Text="add incident"/>
                                <TextBlock   Margin="24,8,24,16" Text="delete incident"/>
                                <Button x:Name="dash">dash</Button>
                                <Button x:Name="inci">incident</Button>

                            </ListView>
                        </Expander>

                    </ListBoxItem>
                    <ListBoxItem>
                        <Expander  HorizontalAlignment="Stretch"  Header="dashboard">
                            <ListView HorizontalAlignment="Right"  ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                                <TextBlock   Margin="24,8,24,16" Text="Incident"/>
                                <TextBlock   Margin="24,8,24,16" Text="add incident"/>
                                <TextBlock   Margin="24,8,24,16" Text="delete incident"/>

                            </ListView>
                        </Expander>
                    </ListBoxItem>

                    <ListBoxItem>

                        <TextBlock    HorizontalAlignment="Stretch" 
                      TextBlock.Foreground="{DynamicResource MaterialDesignBody}"
                         Margin="24,8,24,16"
                    Text="Incident"/>

                    </ListBoxItem>
                </ListBox>
            </StackPanel>
        </Grid>
        <!--end of menu-->
        <!--displaying user control-->
        <Grid Grid.Row="1" Grid.Column="1">
            <ContentControl x:Name="ActiveItem" Margin="10"/>
        </Grid>
        <!--end of displaying user control-->























    </Grid>
</Window>
