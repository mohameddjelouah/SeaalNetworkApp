<Window x:Class="NetworkApp.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NetworkApp.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="ShellView" Height="600" Width="1000">
    <DockPanel>
        
        <StackPanel>
            <ListBox Padding="37 14" FontSize="15" >
                <Expander  HorizontalAlignment="Stretch"  Header="dashboard">
                    <ListView HorizontalAlignment="Right"  ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                        <TextBlock   Margin="24,8,24,16" Text="Incident"/>
                        <TextBlock   Margin="24,8,24,16" Text="add incident"/>
                        <TextBlock   Margin="24,8,24,16" Text="delete incident"/>

                    </ListView>
                </Expander>

                <Expander  HorizontalAlignment="Stretch"  Header="dashboard">
                    <ListView HorizontalAlignment="Right"  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                     
                        <TextBlock   Margin="24,8,24,16" Text="Incident"/>
                        <TextBlock   Margin="24,8,24,16" Text="add incident"/>
                        <TextBlock   Margin="24,8,24,16" Text="delete incident"/>
                        <Button x:Name="dash">dash</Button>
                        <Button x:Name="inci">incident</Button>

                    </ListView>
                </Expander>

                <TextBlock    HorizontalAlignment="Stretch" 
                      TextBlock.Foreground="{DynamicResource MaterialDesignBody}"
                         Margin="24,8,24,16"
                    Text="Incident"/>

            </ListBox>
        </StackPanel>
        
        <Grid>
            <ContentControl x:Name="ActiveItem" Margin="10"/>
        </Grid>











       
            
            
       
        
        
        
        
        
        
        
        
        
    </DockPanel>
</Window>
