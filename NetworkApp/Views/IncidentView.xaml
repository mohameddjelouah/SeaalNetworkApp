<UserControl x:Class="NetworkApp.Views.IncidentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NetworkApp.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" Background="White"
             d:DesignHeight="650" d:DesignWidth="1020">

   
    <Grid>
        
        
        <Grid.ColumnDefinitions>
            
            <ColumnDefinition Width="*"/>
           

        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
           
           
            <RowDefinition Height="*"/>
            

        </Grid.RowDefinitions>

        <DataGrid x:Name="dataincident" SelectedItem="{Binding SelectedIncident, Mode=TwoWay}" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True" AutoGenerateColumns="False" Grid.Column="0" Grid.Row="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date d'incident" Binding="{Binding Path=IncidentDate ,StringFormat=d}"/>
                <DataGridTextColumn Header="Direction" Binding="{Binding Path=Direction}"/>
                <DataGridTextColumn Header="Site" Binding="{Binding Path=Site}"/>
                <DataGridTextColumn Header="Nature" Binding="{Binding Path=Nature}"/>
                <DataGridTextColumn Header="Operateur" Binding="{Binding Path=Operateur}"/>
                <DataGridCheckBoxColumn Header="Cloture d'incident" Binding="{Binding Path=isClotured}"/>


                <materialDesign:MaterialDataGridTextColumn MaxWidth="300" Header="Solution" Binding="{Binding Path=Solution}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextBlock.TextWrapping" Value="Wrap" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </materialDesign:MaterialDataGridTextColumn>



                <materialDesign:MaterialDataGridTextColumn Header="Date Cloture" Binding="{Binding Path=ClotureDate,StringFormat=d}"/>
                <materialDesign:MaterialDataGridTextColumn Header="Ajouter par:" Binding="{Binding Path=AddBy}"/>
                <!--<DataGridTemplateColumn Header="EDIT">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button x:Name="edit">Edit</Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>-->

                <DataGridTemplateColumn Header="DELETE">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button x:Name="delete" Command="{Binding Path=DataContext.deleteIncident, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"  Background="{x:Null}" BorderBrush="{x:Null}" Width="auto" Height="auto"  >
                                <materialDesign:PackIcon Kind="DeleteForeverOutline" Foreground="Black" Width="auto" Height="auto"  />
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>


            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
